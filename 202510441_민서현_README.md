대학교 성적 분석 및 시각화
이 프로젝트는 엑셀 파일로부터 대학생들의 성적 데이터를 불러와서, 전반적인 점수 분포, 평균 및 표준편차, 전공별 평균과 상위 10% 학생들의 점수를 시각화하는 Python 코드입니다.

사용한 라이브러리
pandas

matplotlib

numpy

주요 기능
엑셀 파일에서 성적 데이터 로드

학생 점수에 대한 막대 그래프 및 평균, 표준편차 시각화

전공별 평균 점수와 상위 10% 학생들의 평균 점수 비교 그래프 생성

코드 설명
1. 데이터 로드 함수 (load_excel_file)
엑셀 파일 경로를 인자로 받아 데이터를 불러옵니다.

is_loaded가 True일 경우에만 엑셀 파일을 로드합니다.

2. 점수 분포 시각화 함수 (plot_score_bar)
모든 학생의 점수를 막대그래프로 표시합니다.

점수 평균을 빨간 점선으로, 표준편차 구간은 노란색 음영으로 표시합니다.

3. 전공별 상위 10% 학생 추출 함수 (get_top_10_percent)
전공별로 점수가 상위 10%에 해당하는 학생들을 데이터프레임으로 반환합니다.

4. 전공별 평균 점수와 상위 10% 평균 점수 비교 함수 (plot_major_avg_vs_top10)
전공별 전체 학생 평균 점수와 상위 10% 학생 평균 점수를 비교하는 막대그래프를 생성합니다.

실행 방법
Python과 필요한 라이브러리를 설치합니다:

bash
pip install pandas matplotlib numpy
프로젝트 코드를 실행합니다:

bash
python your_script_name.py
대학교 성적 리스트(500명).xlsx 파일이 스크립트가 위치한 경로에 있어야 정상적으로 동작합니다.

참고 사항
한글 폰트는 C:/Windows/Fonts/malgun.ttf 경로를 기본으로 설정했으므로, 환경에 맞게 폰트 경로를 수정할 수 있습니다.

엑셀 파일 내 컬럼명은 '점수'와 '전공'을 포함해야 합니다.