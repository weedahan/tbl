모듈 설명 : 엑셀 기반 데이터 로딩 모듈

개요 및 목적

개요 : pandas 라이브러리를 사용하여 지정된 엑셀파일에서 학생들의 기존 성적 데이터를 읽어와 DataFrame 형태의 데이터 표(table)로 메모리에 준비하는 역할을 합니다.
목적 : 성적 입력, 수정, 분석 작업이 데이터(표)를 기반으로 쉽게 이루어질 수 있도록 데이터의 준비 및 로딩 환경을 구축하는 것입니다.

주요 기능 및 설명

기능 영역 : 주요 역할 : 코드 예시
환경 설정 : 데이터프레임의 콘솔 출력 방식을 설정합니다. : "pd.set_option('display.width', 400)"
파일 경로 : 엑셀 파일의 상대 경로를 정의합니다. : FILE_NAME = '파이썬 기초/student_data.xlsx'
파일 유효성 검사 : 지정된 경로에 엑셀 파일이 실제로 존재하는지 확인하여 프로그램 오류를 방지합니다. : if not os.path.exists(file_name):
데이터 로딩 : pandas의 read_excel() 함수를 사용하여 엑셀 데이터를 data_table 변수에 저장합니다. : data_table = pd.read_excel(file_name)
데이터 출력 : "로드된 데이터의 첫 5줄을 보여주거나, 필요한 컬럼(학번, 이름, 점수 등)만 선택하여 출력합니다." : print(data_table.head())

모듈 사용의 이점

- 데이터 접근 용이성 : 복잡한 파일 입출력 과정 없이, read_excel_file을 통한 한 번의 함수 호출로 모든 엑셀 데이터를 메모리의 표(DataFrame) 형태로 가져올 수 있습니다.

- 유연한 데이터 처리 : data_table 변수는 pandas의 기능을 그대로 사용할 수 있어, 이후 성적 입력 시 새로운 데이터를 추가하거나, 평균 점수를 계산하는 등의 작업을 효율적으로 수행할 수 있는 기반을 마련합니다.

- 오류 방지 : 파일이 없거나 읽을 수 없는 경우에 대비하여 오류 메시지를 출력하고 프로그램이 비정상적으로 종료되는 것을 방지합니다.
