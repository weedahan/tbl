🟦 📌 그래프 시각화 주요 코드 각 함수별 대략적인 설명

1) set_font()

그래프에서 한글이 깨지지 않도록 운영체제별로 폰트를 자동으로 설정해주는 함수.
윈도우면 맑은고딕, 맥이면 애플고딕처럼 자동으로 바꿔준다.

2) convert_grade_to_point()

학점을 숫자 점수(GPA)로 바꿔주는 함수.
예: A+=4.5, B=3.0 이런 식으로 변환해서 학점 평균을 계산할 때 사용한다.

3) find_student()

사용자가 입력한 값을 기준으로 학생을 찾아주는 함수.
입력값이 숫자면 학번 검색, 문자면 이름 검색으로 자동 판단해서 해당 학생 데이터를 찾아준다.

4) show_score_graph()

전체 점수를 정규분포로 그린 뒤, 평균 점수와 내 점수를 시각적으로 비교해주는 메인 그래프 함수.

정규분포 곡선을 크게 그려주고 평균은 파란 점선(AVG), 내 점수는 빨간 실선(YOU)
그래서 정규분포에서 내 위치가 어디인지 딱 보이게 만든 그래프.

5) show_grade_graph()

학점(A+~F) 분포를 막대그래프로 그려주고, 내 학점을 ‘YOU’로 표시해주는 함수.
학점이 어떤 구간에 얼마나 몰려 있는지도 확인 가능하고, 내 위치도 시각적으로 보여준다.

6) top_10_percent()

전공별로 상위 10%에 드는 학생을 계산해서 출력하는 확장 분석 기능.
전공 안에서 점수 기준으로 상위권 학생을 자동으로 찾는다.
(전공 정보가 있을 때만 작동함.)

7) main()

전체 프로그램의 실행 흐름을 담당하는 메인 함수.

엑셀 파일 불러오기, 학생 선택, 정규분포 그래프 출력, 학점 그래프 출력
전공 상위 10% 출력 이 과정을 한 번에 처리한다.

----------

🟦 1. 폰트 설정 부분
📌 코드
def set_font():
    if platform.system() == "Windows":
        plt.rc("font", family="Malgun Gothic")
    elif platform.system() == "Darwin":
        plt.rc("font", family="AppleGothic")
    else:
        plt.rc("font", family="DejaVu Sans")
----------
🎤 대본

그래프를 그릴 때 한글이 깨지는 경우가 있어서, 운영체제마다 자동으로 맞는 글꼴을 적용해주는 함수를 만들었습니다.
윈도우면 맑은 고딕, 맥이면 애플고딕처럼 자동으로 바꿔 줘서 어떤 컴퓨터에서도 글자가 깨지지 않게 만들었습니다.
----------
🟦 2. 학점 → 평점 변환 기능
📌 코드
def convert_grade_to_point(grades):
    table = {"A+":4.5, "A":4.0, "B+":3.5, ... }
    return np.array([table.get(g, 0.0) for g in grades])
----------
🎤 대본

학점 분포에서 전체 GPA 평균을 계산하려면 문자로 된 학점을 숫자로 바꿔야 하잖아요.
그래서 A+는 4.5, B는 3.0 이런 식으로 변환해주는 기능을 넣었습니다.
이 값으로 학점 그래프에서 평균을 표시할 때 사용합니다.
----------
🟦 3. 학생 검색 기능
📌 코드
def find_student(df, query):
    if query.isdigit():
        result = df[df["학번"] == int(query)]
    else:
        result = df[df["이름"] == query]
    return None if result.empty else result.iloc[0]
----------
🎤 대본

학생을 찾을 때 학번으로 입력할 수도 있고, 이름으로 입력할 수도 있기 때문에 입력한 값이 숫자인지 문자인지 자동으로 판별해서 그에 맞게 학생 데이터를 찾아주는 기능입니다.
검색이 잘못되면 None을 반환해서 오류도 방지하도록 했습니다.
----------
🟦 4. 점수 정규분포 시각화 (히스토그램 제거 버전)
📌 코드 주요 부분
x = np.linspace(min(score_list), max(score_list), 300)
pdf = (1/(std*np.sqrt(2*np.pi))) * np.exp(-0.5*((x-avg)/std)**2)
plt.plot(x, pdf, color='orange', linewidth=2)

plt.axvline(avg, color='blue', linestyle='--')
plt.text(avg, max(pdf)*1.05, "AVG")

plt.axvline(my_score, color='red')
plt.text(my_score, max(pdf)*1.05, "YOU")

plt.scatter([my_score], [0], color='red', s=40)
----------
🎤 대본

이 부분이 가장 신경 쓴 점수 시각화입니다.
원래는 막대그래프(히스토그램) 위에 정규분포를 같이 그렸는데, 정규분포가 너무 작게 보이고 그래프가 복잡해서 가독성이 떨어졌습니다.
그래서 정규분포 그래프만 크게 살리고, 그 위에 평균 점수와 내 점수를 선으로 표시하는 방식으로 바꿨습니다.

파란 점선은 평균 점수 ,빨간 실선은 본인 점수 그리고 ‘AVG’, ‘YOU’라고 글자로도 표시해서 정규분포 안에서 내가 어디에 있는지 바로 알 수 있도록 했습니다.
또, 내 점수 위치에는 빨간 점 하나를 찍어서 더 잘 보이게 했습니다.
----------
🟦 5. 학점(Grade) 분포 시각화
📌 코드 주요 부분
plt.bar(unique_grades, counts)
plt.text(idx, counts[idx] + 0.3, "YOU")

🎤 대본

두 번째 그래프는 학점 분포를 막대그래프로 그리는 부분입니다.
각 학점별 학생 수를 자동으로 계산해서 보여주고, 내 학점 위치에 ‘YOU’를 표시해서 학점 분포에서 내가 어디 있는지를 한눈에 알 수 있게 했습니다.
----------
🟦 6. 전공별 상위 10% 기능
📌 코드
threshold = group["점수"].quantile(0.9)
top_students = group[group["점수"] >= threshold]

🎤 대본

전공 정보가 있는 경우에만 작동하는 기능인데, 각 전공 안에서 상위 10%에 드는 학생을 계산해서 보여주는 기능입니다.
데이터 분석의 확장 기능으로 넣었습니다.
----------
🟦 7. 메인 실행 구조
📌 코드
df = pd.read_excel("대학교 성적 리스트 100명.xlsx")
scores = df["점수"].to_numpy()
grades = df["학점"].to_numpy()

🎤 발표 대본

마지막으로 main 부분에서는 엑셀에서 데이터를 불러오고,
학생을 선택하면 정규분포 그래프 → 학점 그래프 → 전공 분석이 순서대로 실행되도록 만들어놨습니다.

사용자가 한 번 입력하면 전체 흐름이 자동으로 진행되기 때문에 직관적으로 성적 데이터를 볼 수 있습니다.
----------
🟦 마무리

시각화 부분은 점수 분포와 학점 분포 안에서 사용자가 자신의 위치를 정확하고 직관적으로 이해할 수 있게 만드는 것이 핵심 목표였습니다.
특히 점수 그래프에서는 정규분포만 강조해서 평균 대비 사용자 위치가 가장 잘 드러나도록 구성했습니다. 이상 발표를 마치겠습니다.
